{% for color in colors%}
{% for i in scales %}
<Style id="s{{color.id}}-{{ forloop.counter0 }}">
  <IconStyle>
    <scale>{{i}}</scale>
    <Icon>
      <href>/static/charts/img/{{color.file_name}}</href>
    </Icon>
  </IconStyle>
</Style>
{% endfor %}
{% endfor %}